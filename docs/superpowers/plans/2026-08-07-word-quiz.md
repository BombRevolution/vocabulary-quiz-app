# 单词拼写测试与复习软件 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 构建一个基于本地 Excel 词库的桌面拼写测试与复习软件（Tkinter + SQLite），支持多词库独立进度、分级判定、错题优先复习。

**Architecture:** 首次运行用 pandas 从内置 .xls 导入词库到 SQLite，之后不再读 Excel。`quiz_logic.py` 提供纯函数（判定/状态迁移/队列构造），可单元测试；`database.py` 封装全部 SQLite 访问；三个 UI 文件（主界面/拼写界面/导入向导）只做展示与交互。数据流：选词库 → build_queue 生成当日队列 → 逐题判定 → apply_result 立即写库。

**Tech Stack:** Python 3.12、Tkinter（内置）、SQLite（内置）、pandas/xlrd/openpyxl（仅导入用）、pytest。

## Global Constraints

- 工作目录：`C:\Users\Administrator\Desktop\单词测试软件`
- Python >= 3.12，仅用标准库 + pandas + xlrd + openpyxl（requirements.txt 中列出）
- 数据文件：根目录 `vocab.db`（SQLite），首次运行自动生成
- 配置：根目录 `config.json`，含 `daily_new_words`(默认50)、`ignore_case`(默认true)、`ignore_punct`(默认false)
- 判定规则：默认忽略大小写、标点严格匹配（用户明确选择）
- 语音语言：所有 UI 文案用简体中文
- 代码不添加注释（除非用户要求）
- 每答一题立即写库，关闭窗口即保存
- 内置词库文件名：`雅思词汇9400词EXCEL词-乱序版.xls`（3列：序号/单词/中文释义）
- 词性标注从释义中正则提取：

```python
import re
def extract_pos(meaning):
    m = meaning if isinstance(meaning, str) else ""
    return ",".join(sorted(set(re.findall(r"([a-z]+)\.", m))))
```

---

### Task 1: 项目骨架 + SQLite 数据层

**Files:**
- Create: `database.py`
- Create: `tests/test_database.py`
- Create: `requirements.txt`
- Create: `.gitignore`

**Interfaces:**
- Produces: `database.get_conn(db_path: str) -> sqlite3.Connection`
- Produces: `database.init_db(conn) -> None`（建全部表）
- Produces: `database.get_setting(conn, key: str, default: str = "") -> str`
- Produces: `database.set_setting(conn, key: str, value: str) -> None`
- Produces: `database.add_book(conn, name: str, source: str) -> int`（返回 book_id）
- Produces: `database.list_books(conn) -> list[dict]`（含 id/name/source/word_count）
- Produces: `database.insert_words(conn, book_id: int, words: list[tuple[str,str,str]]) -> int`（(word, meaning, pos)，按 word 去重，返回插入数）

- [ ] **Step 1: 初始化 git 仓库与依赖清单**

```bash
git init
```

`requirements.txt`：
```
pandas
xlrd
openpyxl
pytest
```

`.gitignore`：
```
__pycache__/
*.pyc
.vocab_test.db
```

- [ ] **Step 2: 写失败的测试**

`tests/test_database.py`：
```python
import os, sqlite3, tempfile
import pytest
import sys, os.path
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
import database

@pytest.fixture()
def conn():
    fd, path = tempfile.mkstemp(suffix=".db")
    os.close(fd)
    c = database.get_conn(path)
    database.init_db(c)
    yield c
    c.close()
    os.remove(path)

def test_init_creates_tables(conn):
    names = {r[0] for r in conn.execute("SELECT name FROM sqlite_master WHERE type='table'").fetchall()}
    assert {"books", "words", "word_state", "daily_log", "settings"} <= names

def test_settings_roundtrip(conn):
    database.set_setting(conn, "daily_new_words", "50")
    assert database.get_setting(conn, "daily_new_words") == "50"
    assert database.get_setting(conn, "missing", "7") == "7"

def test_add_book_and_list(conn):
    bid = database.add_book(conn, "测试词库", "imported")
    books = database.list_books(conn)
    assert books[0]["name"] == "测试词库"
    assert books[0]["source"] == "imported"
    assert books[0]["word_count"] == 0

def test_insert_words_dedup(conn):
    bid = database.add_book(conn, "测试词库", "imported")
    n = database.insert_words(conn, bid, [("apple", "n. 苹果", "n"), ("apple", "n. 苹果", "n"), ("banana", "n. 香蕉", "n")])
    assert n == 2
    cnt = conn.execute("SELECT COUNT(*) FROM words WHERE book_id=?", (bid,)).fetchone()[0]
    assert cnt == 2
    wc = conn.execute("SELECT word_count FROM books WHERE id=?", (bid,)).fetchone()[0]
    assert wc == 2

def test_insert_words_creates_states(conn):
    bid = database.add_book(conn, "测试词库", "imported")
    database.insert_words(conn, bid, [("apple", "n. 苹果", "n")])
    row = conn.execute("SELECT status FROM word_state WHERE book_id=? AND word_id=(SELECT id FROM words WHERE word='apple')", (bid,)).fetchone()
    assert row[0] == "new"
```

- [ ] **Step 3: 运行确认失败**

Run: `python -m pytest tests/test_database.py -v`
Expected: FAIL with `ModuleNotFoundError: No module named 'database'`

- [ ] **Step 4: 实现 database.py**

```python
import sqlite3

SCHEMA = """
CREATE TABLE IF NOT EXISTS books (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT UNIQUE NOT NULL,
    source TEXT NOT NULL,
    created_at TEXT DEFAULT (datetime('now','localtime')),
    word_count INTEGER DEFAULT 0
);
CREATE TABLE IF NOT EXISTS words (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    book_id INTEGER NOT NULL REFERENCES books(id),
    word TEXT NOT NULL,
    meaning TEXT NOT NULL,
    pos TEXT DEFAULT '',
    UNIQUE(book_id, word)
);
CREATE TABLE IF NOT EXISTS word_state (
    book_id INTEGER NOT NULL,
    word_id INTEGER NOT NULL,
    status TEXT DEFAULT 'new',
    wrong_count INTEGER DEFAULT 0,
    review_count INTEGER DEFAULT 0,
    last_result_date TEXT,
    next_review_date TEXT,
    first_quiz_date TEXT,
    priority INTEGER DEFAULT 0,
    PRIMARY KEY(book_id, word_id)
);
CREATE TABLE IF NOT EXISTS daily_log (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    book_id INTEGER NOT NULL,
    date TEXT NOT NULL,
    new_done INTEGER DEFAULT 0,
    correct INTEGER DEFAULT 0,
    wrong INTEGER DEFAULT 0,
    completed INTEGER DEFAULT 0
);
CREATE TABLE IF NOT EXISTS settings (
    key TEXT PRIMARY KEY,
    value TEXT
);
"""

def get_conn(db_path):
    conn = sqlite3.connect(db_path)
    conn.row_factory = sqlite3.Row
    return conn

def init_db(conn):
    conn.executescript(SCHEMA)
    conn.commit()

def get_setting(conn, key, default=""):
    row = conn.execute("SELECT value FROM settings WHERE key=?", (key,)).fetchone()
    return row["value"] if row else default

def set_setting(conn, key, value):
    conn.execute("INSERT INTO settings(key,value) VALUES(?,?) "
                 "ON CONFLICT(key) DO UPDATE SET value=excluded.value", (key, str(value)))
    conn.commit()

def add_book(conn, name, source):
    cur = conn.execute("INSERT INTO books(name, source) VALUES(?,?)", (name, source))
    conn.commit()
    return cur.lastrowid

def list_books(conn):
    return [dict(r) for r in conn.execute("SELECT * FROM books ORDER BY id").fetchall()]

def insert_words(conn, book_id, words):
    inserted = 0
    for word, meaning, pos in words:
        if not word or not str(word).strip():
            continue
        w = str(word).strip()
        cur = conn.execute("INSERT OR IGNORE INTO words(book_id, word, meaning, pos) VALUES(?,?,?,?)",
                           (book_id, w, str(meaning).strip(), pos))
        if cur.rowcount == 0:
            continue
        wid = cur.lastrowid
        conn.execute("INSERT OR IGNORE INTO word_state(book_id, word_id) VALUES(?,?)", (book_id, wid))
        inserted += 1
    conn.execute("UPDATE books SET word_count=? WHERE id=?", (inserted, book_id))
    conn.commit()
    return inserted
```

- [ ] **Step 5: 运行测试确认通过**

Run: `python -m pytest tests/test_database.py -v`
Expected: PASS (5 passed)

- [ ] **Step 6: 提交**

```bash
git add database.py tests/test_database.py requirements.txt .gitignore
git commit -m "feat: SQLite 数据层与骨架"
```

---

### Task 2: 词库解析与导入（Excel/CSV）

**Files:**
- Create: `importer.py`
- Create: `tests/test_importer.py`

**Interfaces:**
- Consumes: `database.insert_words(conn, book_id, words) -> int`
- Produces: `importer.detect_excel(path: str) -> list[str]`（sheet 名列表）
- Produces: `importer.read_sheet(path: str, sheet: str, head_n: int = 10) -> tuple[list[str], list[list[str]]]`（返回表头行 + 前 N 行数据）
- Produces: `importer.load_words(path: str, sheet: str, word_col: int, meaning_col: int) -> list[tuple[str, str]]`
- Produces: `importer.clean_meaning(raw: str) -> tuple[str, str]`（返回 (清洗后释义, pos)）
- Produces: `importer.import_book(conn, name: str, path: str, sheet: str, word_col: int, meaning_col: int) -> int`（新建词库并导入，返回插入数）

- [ ] **Step 1: 写失败的测试**

`tests/test_importer.py`：
```python
import os, sys, tempfile, sqlite3
import pytest
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
import database, importer

CSV_CONTENT = "word,meaning\napple,n. 苹果。\nbanana,n. 香蕉\norange,a. 橙色的\n"

@pytest.fixture()
def csv_path():
    fd, p = tempfile.mkstemp(suffix=".csv")
    with os.fdopen(fd, "w", encoding="utf-8-sig") as f:
        f.write(CSV_CONTENT)
    return p

@pytest.fixture()
def conn():
    fd, path = tempfile.mkstemp(suffix=".db")
    os.close(fd)
    c = database.get_conn(path)
    database.init_db(c)
    yield c
    c.close()
    os.remove(path)

def test_clean_meaning_removes_period_and_extracts_pos():
    m, pos = importer.clean_meaning("v. 分，划分。")
    assert m == "v. 分，划分"
    assert pos == "v"

def test_clean_meaning_handles_bracket_pos():
    m, pos = importer.clean_meaning("n[化].硫化物。")
    assert pos == "n"
    assert "硫化物" in m

def test_read_sheet_csv(csv_path):
    tables = importer.detect_excel(csv_path)
    head = importer.read_sheet(csv_path, tables[0], 3)
    headers, rows = head
    assert headers[0] == "word"
    assert len(rows) == 3

def test_import_book_csv(conn, csv_path):
    n = importer.import_book(conn, "我的词库", csv_path, importer.detect_excel(csv_path)[0], 0, 1)
    assert n == 3
    books = database.list_books(conn)
    assert books[-1]["name"] == "我的词库"
    assert books[-1]["word_count"] == 3
```

- [ ] **Step 2: 运行确认失败**

Run: `python -m pytest tests/test_importer.py -v`
Expected: FAIL with `ModuleNotFoundError: No module named 'importer'`

- [ ] **Step 3: 实现 importer.py**

```python
import re
import pandas as pd
import database

def detect_excel(path):
    if path.lower().endswith(".csv"):
        return ["sheet1"]
    xl = pd.ExcelFile(path)
    return xl.sheet_names

def read_sheet(path, sheet, head_n=10):
    if path.lower().endswith(".csv"):
        df = pd.read_csv(path)
    else:
        df = pd.read_excel(path, sheet_name=sheet)
    headers = [str(c) for c in df.columns]
    rows = df.head(head_n).fillna("").astype(str).values.tolist()
    return headers, rows

def clean_meaning(raw):
    m = str(raw).strip()
    m = re.sub(r"\.$", "", m)
    pos_match = re.search(r"\[?([a-z]+)\]?\.", m)
    pos = pos_match.group(1) if pos_match else ""
    return m, pos

def load_words(path, sheet, word_col, meaning_col):
    if path.lower().endswith(".csv"):
        df = pd.read_csv(path)
    else:
        df = pd.read_excel(path, sheet_name=sheet)
    out = []
    for _, row in df.iterrows():
        w = row.iloc[word_col]
        m = row.iloc[meaning_col]
        if w is None or str(w).strip() == "":
            continue
        out.append((str(w).strip(), str(m) if m is not None else ""))
    return out

def import_book(conn, name, path, sheet, word_col, meaning_col):
    rows = load_words(path, sheet, word_col, meaning_col)
    cleaned = []
    for w, m in rows:
        cm, pos = clean_meaning(m)
        cleaned.append((w, cm, pos))
    bid = database.add_book(conn, name, "imported")
    return database.insert_words(conn, bid, cleaned)
```

- [ ] **Step 4: 运行测试确认通过**

Run: `python -m pytest tests/test_importer.py -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add importer.py tests/test_importer.py
git commit -m "feat: 词库解析与导入"
```

---

### Task 3: 判定纯函数（大小写/标点/编辑距离）

**Files:**
- Create: `quiz_logic.py`
- Create: `tests/test_quiz_judge.py`

**Interfaces:**
- Produces: `quiz_logic.normalize(text: str, ignore_case: bool, ignore_punct: bool) -> str`
- Produces: `quiz_logic.edit_distance(a: str, b: str) -> int`
- Produces: `quiz_logic.judge(user_input: str, correct: str, ignore_case: bool, ignore_punct: bool) -> str`（返回 `"correct"` / `"blur"` / `"wrong"`）

- [ ] **Step 1: 写失败的测试**

`tests/test_quiz_judge.py`：
```python
import os, sys
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
from quiz_logic import normalize, edit_distance, judge

def test_normalize_case():
    assert normalize("Apple", True, False) == "apple"
    assert normalize("Apple", False, False) == "Apple"

def test_normalize_punct():
    assert normalize("A.M.", True, True) == "am"
    assert normalize("A.M.", True, False) == "a.m."

def test_normalize_keeps_hyphen():
    assert normalize("well-known", True, False) == "well-known"

def test_edit_distance_equal():
    assert edit_distance("apple", "apple") == 0

def test_edit_distance_one_off():
    assert edit_distance("appple", "apple") == 1
    assert edit_distance("aplle", "apple") == 1
    assert edit_distance("appl", "apple") == 1

def test_judge_correct_ignores_case():
    assert judge("APPLE", "apple", True, False) == "correct"

def test_judge_punct_strict_default():
    assert judge("AM", "A.M.", True, False) == "wrong"

def test_judge_punct_ignored():
    assert judge("AM", "A.M.", True, True) == "correct"

def test_judge_blur_one_letter():
    assert judge("appple", "apple", True, False) == "blur"

def test_judge_wrong():
    assert judge("banana", "apple", True, False) == "wrong"
```

- [ ] **Step 2: 运行确认失败**

Run: `python -m pytest tests/test_quiz_judge.py -v`
Expected: FAIL with `ImportError: cannot import name 'normalize'`

- [ ] **Step 3: 实现 quiz_logic.py（判定部分）**

```python
import re
import unicodedata

def normalize(text, ignore_case, ignore_punct):
    s = unicodedata.normalize("NFKC", str(text)).strip()
    if ignore_punct:
        s = re.sub(r"[^a-zA-Z0-9\u4e00-\u9fff]", "", s)
    if ignore_case:
        s = s.lower()
    return s

def edit_distance(a, b):
    m, n = len(a), len(b)
    if m == 0:
        return n
    if n == 0:
        return m
    prev = list(range(n + 1))
    for i in range(1, m + 1):
        cur = [i]
        for j in range(1, n + 1):
            cost = 0 if a[i - 1] == b[j - 1] else 1
            cur.append(min(prev[j] + 1, cur[j - 1] + 1, prev[j - 1] + cost))
        prev = cur
    return prev[n]

def judge(user_input, correct, ignore_case, ignore_punct):
    u = normalize(user_input, ignore_case, ignore_punct)
    c = normalize(correct, ignore_case, ignore_punct)
    if u == c:
        return "correct"
    if edit_distance(u, c) == 1:
        return "blur"
    return "wrong"
```

- [ ] **Step 4: 运行测试确认通过**

Run: `python -m pytest tests/test_quiz_judge.py -v`
Expected: PASS (10 passed)

- [ ] **Step 5: 提交**

```bash
git add quiz_logic.py tests/test_quiz_judge.py
git commit -m "feat: 判定函数"
```

---

### Task 4: 状态迁移与复习间隔

**Files:**
- Modify: `quiz_logic.py`（追加函数）
- Create: `tests/test_quiz_state.py`

**Interfaces:**
- Consumes: `quiz_logic.judge(...) -> str`
- Produces: `quiz_logic.next_review_date(status: str, today: str) -> str`（today 为 `YYYY-MM-DD`，返回复习日期）
- Produces: `quiz_logic.apply_result(state: dict, result: str, today: str) -> dict`（state 含 status/wrong_count/review_count/priority/first_quiz_date，返回更新后的 dict）

- [ ] **Step 1: 写失败的测试**

`tests/test_quiz_state.py`：
```python
import os, sys
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
from quiz_logic import next_review_date, apply_result

T = "2026-08-07"

def test_review_intervals():
    assert next_review_date("poor", T) == "2026-08-08"
    assert next_review_date("blur", T) == "2026-08-10"
    assert next_review_date("good", T) == "2026-08-14"
    assert next_review_date("mastered", T) == "2026-09-06"

def test_correct_promotes_new_to_good():
    s = apply_result({"status": "new", "wrong_count": 0, "review_count": 0, "priority": 0, "first_quiz_date": None}, "correct", T)
    assert s["status"] == "good"
    assert s["first_quiz_date"] == T

def test_correct_promotes_poor_to_blur():
    s = apply_result({"status": "poor", "wrong_count": 1, "review_count": 0, "priority": 5, "first_quiz_date": T}, "correct", T)
    assert s["status"] == "blur"

def test_correct_promotes_blur_to_good():
    s = apply_result({"status": "blur", "wrong_count": 1, "review_count": 0, "priority": 5, "first_quiz_date": T}, "correct", T)
    assert s["status"] == "good"

def test_correct_masters_good():
    s = apply_result({"status": "good", "wrong_count": 0, "review_count": 5, "priority": 0, "first_quiz_date": T}, "correct", T)
    assert s["status"] == "mastered"
    assert s["next_review_date"] == "2026-09-06"

def test_blur_demotes_any():
    s = apply_result({"status": "mastered", "wrong_count": 0, "review_count": 9, "priority": 0, "first_quiz_date": T}, "blur", T)
    assert s["status"] == "blur"

def test_wrong_demotes_to_poor():
    s = apply_result({"status": "mastered", "wrong_count": 0, "review_count": 9, "priority": 0, "first_quiz_date": T}, "wrong", T)
    assert s["status"] == "poor"
    assert s["wrong_count"] == 1
    assert s["priority"] >= 5
    assert s["next_review_date"] == "2026-08-08"

def test_review_count_increments():
    s = apply_result({"status": "new", "wrong_count": 0, "review_count": 0, "priority": 0, "first_quiz_date": None}, "correct", T)
    assert s["review_count"] == 1
```

- [ ] **Step 2: 运行确认失败**

Run: `python -m pytest tests/test_quiz_state.py -v`
Expected: FAIL

- [ ] **Step 3: 实现状态迁移函数**

追加到 `quiz_logic.py`：
```python
from datetime import date, timedelta

REVIEW_INTERVALS = {"poor": 1, "blur": 3, "good": 7, "mastered": 30}

def next_review_date(status, today):
    d = date.fromisoformat(today) + timedelta(days=REVIEW_INTERVALS[status])
    return d.isoformat()

def apply_result(state, result, today):
    s = dict(state)
    s["review_count"] = s.get("review_count", 0) + 1
    if s.get("first_quiz_date") is None and result != "correct":
        s["first_quiz_date"] = today
    if result == "correct":
        if s["status"] == "new":
            s["status"] = "good"
        elif s["status"] == "poor":
            s["status"] = "blur"
        elif s["status"] == "blur":
            s["status"] = "good"
        elif s["status"] == "good":
            s["status"] = "mastered"
        s["priority"] = max(0, s.get("priority", 0) - 1)
        if s.get("first_quiz_date") is None:
            s["first_quiz_date"] = today
    elif result == "blur":
        s["status"] = "blur"
        s["priority"] = s.get("priority", 0) + 2
    else:
        old = s["status"]
        s["status"] = "poor"
        s["wrong_count"] = s.get("wrong_count", 0) + 1
        s["priority"] = s.get("priority", 0) + 5
        if old == "new":
            s["first_quiz_date"] = today
    s["last_result_date"] = today
    s["next_review_date"] = next_review_date(s["status"], today)
    return s
```

- [ ] **Step 4: 运行测试确认通过**

Run: `python -m pytest tests/test_quiz_state.py -v`
Expected: PASS (8 passed)

- [ ] **Step 5: 提交**

```bash
git add quiz_logic.py tests/test_quiz_state.py
git commit -m "feat: 状态迁移与复习间隔"
```

---

### Task 5: 出题队列构造

**Files:**
- Modify: `quiz_logic.py`（追加 `build_queue`）
- Create: `tests/test_quiz_queue.py`

**Interfaces:**
- Consumes: `database.get_setting(conn, "daily_new_words", "50") -> str`
- Produces: `quiz_logic.build_queue(items: list[dict], today: str, daily_new: int) -> list[int]`
  - `items` 为每词一行 dict：`{"id": int, "status": str, "next_review_date": str|None, "priority": int, "first_quiz_date": str|None}`
  - 返回词 id 队列：复习词优先（按 priority 降序），随后最多 `daily_new` 个新词（乱序），复习词与新词交替混合。

- [ ] **Step 1: 写失败的测试**

`tests/test_quiz_queue.py`：
```python
import os, sys
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
from quiz_logic import build_queue

T = "2026-08-07"

def item(i, status, nrd=None, prio=0, fqd=None):
    return {"id": i, "status": status, "next_review_date": nrd, "priority": prio, "first_quiz_date": fqd}

def test_review_words_first():
    items = [
        item(1, "new", None, 0, None),
        item(2, "poor", "2026-08-07", 10, T),
        item(3, "good", "2026-08-06", 3, T),
    ]
    q = build_queue(items, T, 50)
    assert q[0] == 2
    assert 3 in q

def test_due_review_only():
    items = [
        item(2, "poor", "2026-08-07", 10, T),
        item(3, "good", "2026-08-10", 3, T),  # 未到期
    ]
    q = build_queue(items, T, 50)
    assert q == [2]

def test_new_words_limited_by_daily():
    items = [item(i, "new", None, 0, None) for i in range(1, 11)]
    q = build_queue(items, T, 4)
    assert len(q) == 4

def test_new_words_exclude_today_done():
    items = [
        item(1, "new", None, 0, None),
        item(2, "new", None, 0, T),  # 今天已调度过
    ]
    q = build_queue(items, T, 5)
    assert 2 not in q
    assert 1 in q

def test_priority_order_desc():
    items = [
        item(1, "poor", "2026-08-07", 5, T),
        item(2, "poor", "2026-08-07", 20, T),
        item(3, "poor", "2026-08-07", 2, T),
    ]
    q = build_queue(items, T, 0)
    assert q == [2, 1, 3]

def test_empty():
    assert build_queue([], T, 50) == []
```

- [ ] **Step 2: 运行确认失败**

Run: `python -m pytest tests/test_quiz_queue.py -v`
Expected: FAIL

- [ ] **Step 3: 实现 build_queue**

追加到 `quiz_logic.py`：
```python
import random

def build_queue(items, today, daily_new):
    due = [i for i in items
           if i["status"] in ("poor", "blur", "good")
           and i.get("next_review_date") and i["next_review_date"] <= today]
    due.sort(key=lambda i: i["priority"], reverse=True)
    fresh = [i for i in items if i["status"] == "new" and i.get("first_quiz_date") != today]
    random.shuffle(fresh)
    fresh = fresh[:daily_new]
    queue, r, f = [], 0, 0
    while r < len(due) or f < len(fresh):
        if r < len(due):
            queue.append(due[r]["id"]); r += 1
        if f < len(fresh):
            queue.append(fresh[f]["id"]); f += 1
    return queue
```

- [ ] **Step 4: 运行测试确认通过**

Run: `python -m pytest tests/test_quiz_queue.py -v`
Expected: PASS (7 passed)

- [ ] **Step 5: 提交**

```bash
git add quiz_logic.py tests/test_quiz_queue.py
git commit -m "feat: 出题队列构造"
```

---

### Task 6: 主界面（词库选择 + 统计 + 开始）

**Files:**
- Create: `ui_main.py`
- Create: `main.py`

**Interfaces:**
- Consumes: `database.list_books(conn) -> list[dict]`
- Consumes: `database.insert_words(conn, book_id, words) -> int`
- Consumes: `importer.import_book(conn, name, path, sheet, word_col, meaning_col) -> int`
- Consumes: `quiz_logic.build_queue(items, today, daily_new) -> list[int]`
- Produces: `ui_main.MainApp`（Tkinter 类，构造参数 `(conn, db_path)`）
- Produces: `main.py` 入口：加载 config.json → 连接 vocab.db → init_db → 若 books 为空则自动导入内置 xls → 启动 MainApp

**config.json**（main.py 首次运行生成）：
```json
{"daily_new_words": 50, "ignore_case": true, "ignore_punct": false}
```

- [ ] **Step 1: 写主界面与入口（手动测试，不写 pytest）**

`main.py`：
```python
import json, os
import database

BASE_DIR = os.path.dirname(os.path.abspath(__file__))
DB_PATH = os.path.join(BASE_DIR, "vocab.db")
CONFIG_PATH = os.path.join(BASE_DIR, "config.json")
DEFAULT_XLS = "雅思词汇9400词EXCEL词-乱序版.xls"

def load_config():
    defaults = {"daily_new_words": 50, "ignore_case": True, "ignore_punct": False}
    if os.path.exists(CONFIG_PATH):
        with open(CONFIG_PATH, encoding="utf-8") as f:
            data = json.load(f)
        defaults.update(data)
    else:
        with open(CONFIG_PATH, "w", encoding="utf-8") as f:
            json.dump(defaults, f, ensure_ascii=False, indent=2)
    return defaults

def ensure_builtin(conn):
    if database.list_books(conn):
        return
    xls = os.path.join(BASE_DIR, DEFAULT_XLS)
    if os.path.exists(xls):
        import importer
        sheet = importer.detect_excel(xls)[0]
        importer.import_book(conn, "雅思词汇9400", xls, sheet, 0, 1)

def main():
    import tkinter as tk
    from ui_main import MainApp
    config = load_config()
    conn = database.get_conn(DB_PATH)
    database.init_db(conn)
    ensure_builtin(conn)
    for key, val in config.items():
        database.set_setting(conn, key, val)
    root = tk.Tk()
    app = MainApp(root, conn, DB_PATH, config)
    root.mainloop()
    conn.close()

if __name__ == "__main__":
    main()
```

`ui_main.py`：
```python
import tkinter as tk
from tkinter import messagebox
from tkinter import ttk
import database

class MainApp:
    def __init__(self, root, conn, db_path, config):
        self.root = root
        self.conn = conn
        self.db_path = db_path
        self.config = config
        root.title("单词拼写测试")
        root.geometry("760x520")
        self._build_ui()
        self.refresh()

    def _build_ui(self):
        main = ttk.Frame(self.root, padding=16)
        main.pack(fill="both", expand=True)
        left = ttk.Frame(main)
        left.pack(side="left", fill="y", padx=(0, 16))
        ttk.Label(left, text="词库").pack(anchor="w")
        self.book_list = tk.Listbox(left, width=26, height=18)
        self.book_list.pack(fill="y")
        self.book_list.bind("<<ListboxSelect>>", lambda e: self.refresh())
        btn_row = ttk.Frame(left)
        btn_row.pack(fill="x", pady=6)
        ttk.Button(btn_row, text="导入词库", command=self.open_import).pack(side="left", padx=(0, 6))
        ttk.Button(btn_row, text="设置", command=self.open_settings).pack(side="left")

        right = ttk.Frame(main)
        right.pack(side="left", fill="both", expand=True)
        self.title = ttk.Label(right, text="", font=("", 16, "bold"))
        self.title.pack(anchor="w")
        self.stats = ttk.Label(right, text="", justify="left", font=("", 12))
        self.stats.pack(anchor="w", pady=12)
        ttk.Button(right, text="开始学习", command=self.start_quiz).pack(anchor="w", ipadx=24, ipady=8)

    def current_book(self):
        sel = self.book_list.curselection()
        if not sel:
            return None
        return self.books[sel[0]]

    def refresh(self):
        self.books = database.list_books(self.conn)
        self.book_list.delete(0, "end")
        for b in self.books:
            self.book_list.insert("end", f"{b['name']}（{b['word_count']}词）")
        if self.books:
            self.book_list.selection_set(0)
        self.update_stats()

    def update_stats(self):
        book = self.current_book()
        if not book:
            self.title.config(text="暂无词库")
            self.stats.config(text="请先导入词库")
            return
        self.title.config(text=f"当前词库：{book['name']}")
        counts = self._state_counts(book["id"])
        txt = (f"今日新词：{counts['new_done_today']} / {self.config['daily_new_words']}\n"
               f"待复习：{counts['due']}\n"
               f"错题总数：{counts['wrong_total']}\n"
               f"已掌握：{counts['mastered']}\n"
               f"共 {book['word_count']} 词")
        self.stats.config(text=txt)

    def _state_counts(self, book_id):
        due_rows = self.conn.execute(
            "SELECT COUNT(*) c FROM word_state ws JOIN words w ON w.id=ws.word_id "
            "WHERE ws.book_id=? AND ws.status IN ('poor','blur','good') "
            "AND ws.next_review_date IS NOT NULL AND ws.next_review_date<=date('now','localtime')",
            (book_id,)).fetchone()
        wrong = self.conn.execute(
            "SELECT COALESCE(SUM(ws.wrong_count),0) FROM word_state ws WHERE ws.book_id=?",
            (book_id,)).fetchone()[0]
        mast = self.conn.execute(
            "SELECT COUNT(*) FROM word_state WHERE book_id=? AND status='mastered'",
            (book_id,)).fetchone()[0]
        new_done = self.conn.execute(
            "SELECT COUNT(*) FROM word_state WHERE book_id=? AND first_quiz_date=date('now','localtime')",
            (book_id,)).fetchone()[0]
        return {"due": due_rows["c"], "wrong_total": wrong, "mastered": mast, "new_done_today": new_done}

    def start_quiz(self):
        book = self.current_book()
        if not book:
            return
        from ui_quiz import QuizApp
        QuizApp(self.root, self.conn, book, self.config)

    def open_import(self):
        from ui_import import ImportDialog
        ImportDialog(self.root, self.conn)

    def open_settings(self):
        from ui_settings import SettingsDialog
        SettingsDialog(self.root, self.config, self.db_path)
```

- [ ] **Step 2: 手动冒烟测试**

Run: `python main.py`
Expected: 窗口打开，自动导入雅思词汇词库，左侧列表显示"雅思词汇9400（9389词）"，统计面板显示数值，"开始学习"按钮存在。手动关闭窗口无报错。

- [ ] **Step 3: 提交**

```bash
git add main.py ui_main.py config.json vocab.db
git commit -m "feat: 主界面与程序入口"
```

---

### Task 7: 拼写测试界面

**Files:**
- Create: `ui_quiz.py`

**Interfaces:**
- Consumes: `database.get_setting`, `quiz_logic.build_queue`, `quiz_logic.judge`, `quiz_logic.apply_result`
- Consumes: `MainApp`（构造参数 `(root, conn, book, config)`）
- Produces: `ui_quiz.QuizApp(root: tk.Tk, conn, book: dict, config: dict)`——新建 Toplevel 窗口，跑完队列后销毁并回调刷新主界面。

- [ ] **Step 1: 写拼写界面**

`ui_quiz.py`：
```python
import tkinter as tk
from tkinter import ttk
from datetime import date
import database
import quiz_logic

class QuizApp:
    def __init__(self, root, conn, book, config):
        self.conn = conn
        self.book = book
        self.config = config
        self.today = date.today().isoformat()
        self.daily_new = int(config.get("daily_new_words", 50))
        self.retry_pool = []

        self.win = tk.Toplevel(root)
        self.win.title(f"拼写测试 - {book['name']}")
        self.win.geometry("640x480")
        self.win.grab_set()

        self.items = self._load_queue()
        if not self.items:
            tk.messagebox.showinfo("提示", "今日词汇已全部完成，明天再来！")
            self.win.destroy()
            return
        self.queue = list(self.items)
        self.total = len(self.queue)
        self.idx = 0
        self.stats = {"correct": 0, "blur": 0, "wrong": 0}

        self._build_ui()
        self._show_next()

    def _load_queue(self):
        rows = self.conn.execute(
            "SELECT w.id, w.word, w.meaning, w.pos, ws.status, ws.next_review_date, "
            "ws.priority, ws.first_quiz_date, ws.wrong_count, ws.review_count "
            "FROM words w JOIN word_state ws ON ws.word_id=w.id AND ws.book_id=w.book_id "
            "WHERE w.book_id=? ORDER BY w.id",
            (self.book["id"],)).fetchall()
        items = []
        for r in rows:
            items.append({
                "id": r["id"], "word": r["word"], "meaning": r["meaning"], "pos": r["pos"],
                "status": r["status"], "next_review_date": r["next_review_date"],
                "priority": r["priority"], "first_quiz_date": r["first_quiz_date"],
                "wrong_count": r["wrong_count"], "review_count": r["review_count"],
            })
        ids = quiz_logic.build_queue(items, self.today, self.daily_new)
        by_id = {i["id"]: i for i in items}
        return [by_id[i] for i in ids]

    def _build_ui(self):
        self.prog = ttk.Progressbar(self.win, maximum=self.total)
        self.prog.pack(fill="x", padx=16, pady=(16, 4))
        self.prog_label = ttk.Label(self.win, text="")
        self.prog_label.pack()
        self.meaning_label = tk.Label(self.win, text="", font=("", 18), wraplength=560)
        self.meaning_label.pack(pady=24)
        self.entry = ttk.Entry(self.win, font=("", 16), justify="center")
        self.entry.pack(ipady=6)
        self.entry.bind("<Return>", lambda e: self.submit())
        self.feedback = ttk.Label(self.win, text="", font=("", 14))
        self.feedback.pack(pady=16)
        self.hint = ttk.Label(self.win, text="", font=("", 12, "italic"))
        self.hint.pack()
        bottom = ttk.Frame(self.win)
        bottom.pack(side="bottom", fill="x", padx=16, pady=12)
        ttk.Button(bottom, text="退出并保存", command=self.finish).pack(side="right")
        self.remain = ttk.Label(bottom, text="")
        self.remain.pack(side="left")

    def _state(self, item):
        return self.conn.execute(
            "SELECT status, wrong_count, review_count, priority, first_quiz_date "
            "FROM word_state WHERE book_id=? AND word_id=?",
            (self.book["id"], item["id"])).fetchone()

    def _save(self, item, result):
        st = self._state(item)
        state = dict(st) if st else {"status": "new", "wrong_count": 0, "review_count": 0,
                                     "priority": 0, "first_quiz_date": None}
        new = quiz_logic.apply_result(state, result, self.today)
        self.conn.execute(
            "UPDATE word_state SET status=?, wrong_count=?, review_count=?, priority=?, "
            "first_quiz_date=?, last_result_date=?, next_review_date=? WHERE book_id=? AND word_id=?",
            (new["status"], new["wrong_count"], new["review_count"], new["priority"],
             new.get("first_quiz_date"), new.get("last_result_date"), new.get("next_review_date"),
             self.book["id"], item["id"]))
        self.conn.commit()

    def _show_next(self):
        if self.idx >= self.total:
            self.finish()
            return
        item = self.queue[self.idx]
        self.prog["value"] = self.idx
        self.prog_label.config(text=f"第 {self.idx + 1} / {self.total} 题")
        self.remain.config(text=f"剩余 {self.total - self.idx} 题")
        meaning = item["meaning"]
        self.meaning_label.config(text=meaning)
        self.feedback.config(text="", foreground="black")
        self.hint.config(text="")
        self.entry.delete(0, "end")
        self.entry.focus_set()

    def submit(self):
        user = self.entry.get()
        if not user.strip():
            return
        item = self.queue[self.idx]
        result = quiz_logic.judge(user, item["word"], self.config.get("ignore_case", True),
                                  self.config.get("ignore_punct", False))
        self.stats[result] += 1
        self._save(item, result)
        if result == "correct":
            self.feedback.config(text=f"✓ 正确：{item['word']}", foreground="#2e7d32")
            self.idx += 1
            self.win.after(600, self._show_next)
        elif result == "blur":
            self.feedback.config(text=f"很接近！正确拼写是 {item['word']}", foreground="#e65100")
            self.hint.config(text="本词将稍后重练")
            self.retry_pool.append(self.queue[self.idx])
            self.idx += 1
            self.win.after(1200, self._show_next)
        else:
            self.feedback.config(text=f"✗ 错误，正确拼写是 {item['word']}", foreground="#c62828")
            self.hint.config(text="本词将稍后重练")
            self.retry_pool.append(self.queue[self.idx])
            self.idx += 1
            self.win.after(1200, self._show_next)
        if self.retry_pool and self.idx >= self.total:
            self.queue.extend(self.retry_pool)
            self.retry_pool = []
            self.total = len(self.queue)

    def finish(self):
        tk.messagebox.showinfo("本次小结",
            f"正确 {self.stats['correct']} 题，模糊 {self.stats['blur']} 题，错误 {self.stats['wrong']} 题")
        self.win.destroy()
```

- [ ] **Step 2: 手动冒烟测试**

Run: `python main.py` → 点"开始学习"
Expected: 显示中文释义、输入框可输入、回车判定绿/红显示、答错词稍后重练、进度条步进、结束弹小结、返回主界面统计刷新。

- [ ] **Step 3: 提交**

```bash
git add ui_quiz.py
git commit -m "feat: 拼写测试界面"
```

---

### Task 8: 导入向导与设置窗口

**Files:**
- Create: `ui_import.py`
- Create: `ui_settings.py`

**Interfaces:**
- Consumes: `importer.detect_excel`, `importer.read_sheet`, `importer.import_book`
- Consumes: 主界面 `refresh()` 方法
- Produces: `ui_import.ImportDialog(root: tk.Tk, conn)`——modal 对话框，完成后回调刷新
- Produces: `ui_settings.SettingsDialog(root: tk.Tk, config: dict, db_path: str)`——读写 config.json

- [ ] **Step 1: 写导入向导**

`ui_import.py`：
```python
import tkinter as tk
from tkinter import ttk, filedialog, messagebox
import importer

class ImportDialog(tk.Toplevel):
    def __init__(self, root, conn):
        super().__init__(root)
        self.conn = conn
        self.title("导入词库")
        self.geometry("560x420")
        self.grab_set()
        self.path = None
        self.sheets = []
        rows = ttk.Frame(self, padding=12)
        rows.pack(fill="both", expand=True)

        ttk.Button(rows, text="选择文件", command=self.pick_file).grid(row=0, column=0, sticky="w", pady=4)
        self.file_label = ttk.Label(rows, text="未选择文件")
        self.file_label.grid(row=0, column=1, sticky="w", columnspan=2)
        ttk.Label(rows, text="工作表").grid(row=1, column=0, sticky="w", pady=4)
        self.sheet_var = tk.StringVar()
        self.sheet_box = ttk.Combobox(rows, textvariable=self.sheet_var, state="readonly", width=24)
        self.sheet_box.grid(row=1, column=1, sticky="w", columnspan=2)
        self.sheet_box.bind("<<ComboboxSelected>>", lambda e: self.preview())
        ttk.Label(rows, text="英文单词列").grid(row=2, column=0, sticky="w", pady=4)
        self.word_var = tk.StringVar()
        self.word_box = ttk.Combobox(rows, textvariable=self.word_var, state="readonly", width=24)
        self.word_box.grid(row=2, column=1, sticky="w", columnspan=2)
        ttk.Label(rows, text="中文释义列").grid(row=3, column=0, sticky="w", pady=4)
        self.meaning_var = tk.StringVar()
        self.meaning_box = ttk.Combobox(rows, textvariable=self.meaning_var, state="readonly", width=24)
        self.meaning_box.grid(row=3, column=1, sticky="w", columnspan=2)
        ttk.Label(rows, text="词库名称").grid(row=4, column=0, sticky="w", pady=4)
        self.name_entry = ttk.Entry(rows, width=26)
        self.name_entry.grid(row=4, column=1, sticky="w", columnspan=2)

        self.preview_tree = ttk.Treeview(rows, height=8, show="headings")
        self.preview_tree.grid(row=5, column=0, columnspan=3, sticky="nsew", pady=8)
        rows.rowconfigure(5, weight=1)
        ttk.Button(rows, text="确认导入", command=self.do_import).grid(row=6, column=0, columnspan=3, pady=8)

    def pick_file(self):
        path = filedialog.askopenfilename(filetypes=[("词库文件", "*.xls *.xlsx *.csv")])
        if not path:
            return
        self.path = path
        self.file_label.config(text=path)
        try:
            self.sheets = importer.detect_excel(path)
        except Exception as e:
            messagebox.showerror("错误", f"无法读取文件：{e}")
            return
        self.sheet_box["values"] = self.sheets
        self.sheet_var.set(self.sheets[0])
        self.preview()

    def preview(self):
        head, rows = importer.read_sheet(self.path, self.sheet_var.get(), 8)
        cols = [f"{i}: {h}" for i, h in enumerate(head)]
        self.word_box["values"] = cols
        self.meaning_box["values"] = cols
        self.preview_tree.delete(*self.preview_tree.get_children())
        self.preview_tree["columns"] = [str(i) for i in range(len(head))]
        for i, h in enumerate(head):
            self.preview_tree.heading(str(i), text=h)
        for r in rows:
            self.preview_tree.insert("", "end", values=r)

    def do_import(self):
        if not self.path:
            messagebox.showwarning("提示", "请先选择文件")
            return
        name = self.name_entry.get().strip()
        if not name:
            messagebox.showwarning("提示", "请输入词库名称")
            return
        try:
            wc = int(self.word_var.get().split(":")[0])
            mc = int(self.meaning_var.get().split(":")[0])
        except (ValueError, IndexError):
            messagebox.showwarning("提示", "请选择英文单词列和中文释义列")
            return
        try:
            n = importer.import_book(self.conn, name, self.path, self.sheet_var.get(), wc, mc)
            messagebox.showinfo("导入成功", f"已导入 {n} 个单词")
            self.destroy()
        except Exception as e:
            messagebox.showerror("导入失败", f"导入出错：{e}")
```

- [ ] **Step 2: 写设置窗口**

`ui_settings.py`：
```python
import json
import tkinter as tk
from tkinter import ttk

class SettingsDialog(tk.Toplevel):
    def __init__(self, root, config, db_path):
        super().__init__(root)
        self.config = config
        self.db_path = db_path
        self.title("设置")
        self.geometry("360x220")
        self.grab_set()
        rows = ttk.Frame(self, padding=16)
        rows.pack(fill="both", expand=True)
        ttk.Label(rows, text="每日新词数").grid(row=0, column=0, sticky="w", pady=6)
        self.daily_var = tk.IntVar(value=int(config.get("daily_new_words", 50)))
        ttk.Spinbox(rows, from_=1, to=500, textvariable=self.daily_var, width=10).grid(row=0, column=1, sticky="w")
        ttk.Label(rows, text="忽略大小写").grid(row=1, column=0, sticky="w", pady=6)
        self.case_var = tk.BooleanVar(value=config.get("ignore_case", True))
        ttk.Checkbutton(rows, variable=self.case_var).grid(row=1, column=1, sticky="w")
        ttk.Label(rows, text="忽略标点").grid(row=2, column=0, sticky="w", pady=6)
        self.punct_var = tk.BooleanVar(value=config.get("ignore_punct", False))
        ttk.Checkbutton(rows, variable=self.punct_var).grid(row=2, column=1, sticky="w")
        ttk.Button(rows, text="保存", command=self.save).grid(row=3, column=0, columnspan=2, pady=12)

    def save(self):
        self.config["daily_new_words"] = self.daily_var.get()
        self.config["ignore_case"] = self.case_var.get()
        self.config["ignore_punct"] = self.punct_var.get()
        with open(self.db_path.replace("vocab.db", "config.json"), "w", encoding="utf-8") as f:
            json.dump(self.config, f, ensure_ascii=False, indent=2)
        self.destroy()
```

- [ ] **Step 3: 手动冒烟测试**

Run: `python main.py`
- 点"导入词库"：选文件 → 出现工作表/列下拉 → 选列 → 输入名称 → 确认 → 提示导入成功，主界面出现新词库。
- 点"设置"：改每日新词数 → 保存 → 重新打开确认已保存。
Expected: 全部正常，无异常。

- [ ] **Step 4: 提交**

```bash
git add ui_import.py ui_settings.py
git commit -m "feat: 导入向导与设置窗口"
```

---

### Task 9: 全流程验证与收尾

**Files:**
- Modify: 各文件（如有 bug）

- [ ] **Step 1: 运行全部单元测试**

Run: `python -m pytest tests/ -v`
Expected: 全部 PASS（30 个左右）

- [ ] **Step 2: 端到端手动验证**

1. 删除 `vocab.db` 和 `config.json`，运行 `python main.py` → 自动导入内置词库。
2. 开始学习：输入正确词 → 绿"正确"自动跳题；输入近似词 → 橙"很接近"稍后重练；输入错误词 → 红字显示正确拼写、稍后重练。
3. 中途"退出并保存" → 重新打开 → 错题出现在队列最前。
4. 统计面板数值随作答变化。
5. 导入向导导入一个 CSV 词库 → 出现在词库列表，进度独立。
6. 设置每日新词数=10 → 重新学习 → 新词只出 10 个。
Expected: 以上全部符合预期。

- [ ] **Step 3: 提交**

```bash
git add -A
git commit -m "chore: 全流程验证通过"
```

---

## Self-Review 记录

- **Spec 覆盖**：多词库(spec §4) → Task 1/2/8；判定(spec §5.2) → Task 3；状态迁移(spec §5.3) → Task 4；队列(spec §5.1) → Task 5；主界面统计(spec §6.1) → Task 6；拼写界面(spec §6.2) → Task 7；导入向导(spec §6.4)+设置(spec §6.3) → Task 8；错误处理(spec §7) → Task 6/8 的 try-except + 空队列提示；测试(spec §8) → Task 1-5 的 pytest。全部覆盖。
- **占位符扫描**：无 TBD/TODO，所有代码块为完整实现。
- **类型一致性**：`build_queue(items, today, daily_new)`、`apply_result(state, result, today)`、`judge(user_input, correct, ignore_case, ignore_punct)`、`import_book(conn, name, path, sheet, word_col, meaning_col)` 在定义与使用处签名一致；`database.insert_words(conn, book_id, words)` 返回 int 被 Task 2 使用一致。